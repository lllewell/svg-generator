const Shape = require('./shapes.js')

describe('Shape', () => {
    describe('Circle', () => {
        it('renders a circle', () => {
            const shape = new Shape();
            expect(shape.circle).toBe(shape);
        });
    });
    describe('Square', () => {
        it('renders a square', () => {
            const shape = new Shape();
            expect().toBe();
        });
    });
    describe('Triangle', () => {
        it('renders a triangle', () => {
            const shape = new Shape();
            expect().toBe();

        });
    });
});